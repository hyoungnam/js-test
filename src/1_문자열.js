// SpecRunner를 활용하여서 조건을 만족시키세요.

/** 
 * # 01_getFullName
 * 
 * Write a function called "getFullName". 
 * ("getFullName" 함수를 작성하세요.)
 * 
 * Given a first and a last name, "getFullName" returns 
 * a single string with the given first and last names 
 * separated by a single space. 
 * (이름과 성이 주어졌을때, "getFullName" 함수는 이름과 성이 띄어쓰기 하나를 
 * 사이에 둔 단일 문자열을 반환해야 합니다.)
 * 
 * let output = getFullName('Joe', 'Smith');
 * console.log(output); // --> 'Joe Smith'
*/
function getFullName(firstName, lastName) { 
  
}

/** 
 * 
 * # 02_getLengthOfWord
 * 
 * Write a function called "getLengthOfWord". 
 * ("getLengthOfWord" 함수를 작성하세요.)
 * 
 * Given a word, "getLengthOfWord" returns the 
 * length of the given word. 
 * (단어가 주어졌을때, "getLengthOfWord" 함수는 단어의 길이를 반환해야 합니다.)
 * 
 * let output = getLengthOfWord('some');
 * console.log(output); // --> 4

*/
function getLengthOfWord(word) {
  
}

/** 
 * Write a function called "computeAverageLengthOfWords". 
 * ("computeAverageLengthOfWords" 함수를 작성하세요.)
 * 
 * Given two words, "computeAverageLengthOfWords" 
 * returns the average of their lengths. 
 * (두 단어가 주어졌을때, "computeAverageLengthOfWords" 
 * 함수는 두 단어 길이의 평균값을 반환합니다.)
 * 
 * 
 * let output = computeAverageLengthOfWords('code', 'programs');
 * console.log(output); // --> 6
 * 
*/
function computeAverageLengthOfWords(word1, word2) {
  
}

/** 
 * # 04_isOddLength
 * Write a function called "isOddLength". 
 * ("isOddLength" 함수를 작성하세요.)
 * 
 * Given a word, "isOddLength" returns whether the length of 
 * the given word is odd. 
 * (단어가 주어졌을때, "isOddLength" 함수는 주어진 단어의 길이가 
 * 홀수인지 반환해야 합니다.)
 * 
 * let output = isOddLength('special');
 * console.log(output); // --> true
 * 
*/

function isOddLength(word) {
  
}


/** 
 * # 05_isEvenLength
 * Write a function called "isEvenLength". 
 * ("isEvenLength" 함수를 작성하세요.)
 * 
 * Given a word, "isEvenLength" returns whether the length 
 * of the word is even. 
 * (단어가 주어졌을때, "isEvenLength" 함수는 
 * 주어진 단어의 길이가 짝수인지 반환해야 합니다.)
 * 
 * let output = isEvenLength('wow');
 * console.log(output); // --> false
 * 
*/

function isEvenLength(word) {
  
}



/** 
 * # 06_repeatString
 * Write a function called "repeatString". 
 * ("repeatString" 함수를 작성하세요.)
 * 
 * Given a string and a number, "repeatString" 
 * returns the given string repeated the given number of times. 
 * (문자열과 숫자가 주어졌을때, "repeatString" 함수는 주어진 
 * 문자열을 주어진 숫자만큼 반복하여 반환해야 합니다.)
 * 
 * let output = repeatString('code', 3);
 * console.log(output); // --> 'codecodecode'
 
*/

function repeatString(string, num) {
  
}

/** 
 * # 07_findShortestOfThreeWords
 * Write a function called "findShortestOfThreeWords". 
 * ("findShortestOfThreeWords" 함수를 작성하세요.)
 * 
 * Given 3 strings, "findShortestOfThreeWords" returns 
 * the shortest of the given strings. 
 * (문자열 3개가 주어졌을때, "findShortestOfThreeWords" 
 * 함수는 주어진 문자열 중 가장 짧은 문자열을 반환해야 합니다.)
 * 
 * Notes:
 * If there are ties, it should return the first word 
 * in the parameters list. 
 * (만약 동률이 있다면, 그 중 앞에 있는 문자열을 반환해야 합니다.)
 * 
 * let output = findShortestOfThreeWords('a', 'two', 'three');
 * console.log(output); // --> 'a'
*/

function findShortestOfThreeWords(word1, word2, word3) {
  
}


/** 
 * # 08_countCharacter
 * Write a function called "countCharacter". 
 * ("countCharacter" 함수를 작성하세요.)
 * 
 * Given a string input and a character, "countCharacter" 
 * returns the number of occurences of a given 
 * character in the given string. 
 * (문자열과 문자가 주어졌을때, "countCharacter" 함수는 
 * 주어진 문자열에서 주어진 문자가 몇개가 있는지를 반환해야 합니다.)
 * 
 * let output = countCharacter('I am a hacker', 'a');
 * console.log(output); // --> 3
*/

function countCharacter(str, char) {
  
}


/** 
 * # 09_areValidCredentials
 * Write a function called "areValidCredentials". 
 * ("areValidCredentials" 함수를 작성하세요.)
 * 
 * Given a name and a password, "areValidCredentials", 
 * returns true if the name is longer than 3 characters, AND, 
 * the password is at least 8 characters long. 
 * Otherwise it returns false. 
 * (이름과 비밀번호가 주어졌을때, "areValidCredentials" 
 * 함수는 이름이 3글자보다 길고 비밀번호가 8글자 이상이면 true를 반환합니다.)
 * 
 * let output = areValidCredentials('Ritu', 'mylongpassword')
 * console.log(output); // --> true
*/

function areValidCredentials(name, password) {
  
}



/** 
 * # 10_findMinLengthOfThreeWords
 * Write a function called "findMinLengthOfThreeWords". 
 * ("findMinLengthOfThreeWords" 함수를 작성하세요.)
 * 
 * Given 3 words, "findMinLengthOfThreeWords" returns the length 
 * of the shortest word. 
 * (단어 세개가 주어졌을때, "findMinLengthOfThreeWords" 
 * 함수는 세 단어 중 가장 짧은 단어의 길이를 반환합니다.)
 * 
 * let output = findMinLengthOfThreeWords('a', 'be', 'see');
 * console.log(output); // --> 1

*/
function findMinLengthOfThreeWords(word1, word2, word3) {
  
}


