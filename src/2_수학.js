// SpecRunner를 활용하여서 조건을 만족시키세요.

/** 
 * # 01_computeAreaOfATriangle
 * 
 * Write a function called "computeAreaOfATriangle". 
 * ("computeAreaOfATriangle" 함수를 작성하세요.)
 * 
 * Given the base and height of a triangle, "computeAreaOfATriangle" 
 * returns its area. 
 * (삼각형의 밑변과 높이가 주어졌을때, "computeAreaOfATriangle" 함수는 
 * 삼각형의 넓이를 반환합니다.)
 * 
 * let output = computeAreaOfATriangle(4, 6);
 * console.log(output); // --> 12
*/

function computeAreaOfATriangle(base, height) {
  
}

/** 
 * # 02_computePower
 * 
 * Write a function called "computePower". 
 * ("computePower" 함수를 작성하세요.)
 * 
 * Given a number and an exponent, "computePower" returns the given 
 * number, raised to the given exponent. 
 * (숫자와 지수가 주어졌을때, "computePower" 
 * 함수는 숫자의 지수만큼 제곱된 값을 반환해야 합니다.)
 * 
 * let output = computePower(2, 3);
 * console.log(output); // --> 8

*/
function computePower(num, exponent) {
  
}

/** 
 * # 03_computePerimeterOfACircle
 * Write a function called "computePerimeterOfACircle". 
 * ("computePerimeterOfACircle" 함수를 작성하세요.)
 * 
 * Given the radius of a circle, "computePerimeterOfACircle" 
 * returns its perimeter. 
 * (원의 반지름이 주어졌을때, "computePerimeterOfACircle" 
 * 함수는 원의 둘레를 반환하세요.)
 * 
 * let output = computePerimeterOfACircle(4);
 * console.log(output); // --> 25.132741228718345
 * 
*/
function computePerimeterOfACircle(radius) {
  
}


/** 
 * # 04_computeAverageOfNumbers

 * Write a function called "computeAverageOfNumbers".
 * ("computeAverageOfNumbers" 함수를 작성하세요.)
  
 * Given an array of numbers, "computeAverageOfNumbers" 
 * returns their average.
 * (숫자이 배열이 주어졌을때, "computeAverageOfNumbers" 함수는 
 * 그들의 평균을 반환합니다.)
 
 * Notes:
 * - If given an empty array, it should return 0. 
 * (만약 빈배열이 주어졌다면, 0을 반환해야 합니다.)
 * 
 * let input = [1,2,3,4,5];
 * let output = computeAverageOfNumbers(input);
 * console.log(output); // --> 3
*/

function computeAverageOfNumbers(nums) {
  
}

